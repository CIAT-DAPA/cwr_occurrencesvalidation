CREATE TABLE `cwr_gapanalysis`.`temp_countries` (
  `name` VARCHAR(50) NOT NULL,
  `iso2` VARCHAR(2) NOT NULL,
  `iso3` VARCHAR(3) NOT NULL,
  PRIMARY KEY (`name`));

CREATE TABLE `temp_occurrences` (
  `id` int(12) NOT NULL AUTO_INCREMENT COMMENT 'record identifacation number',
  `old_id` int(12) DEFAULT NULL,
  `taxon_id` int(11) DEFAULT NULL COMMENT 'Taxon identifier for linkage with Species table',
  `metadata_id` int(11) DEFAULT NULL,
  `field_collected_data` tinyint(1) DEFAULT NULL COMMENT 'Boolean field (1=yes, 0=no). Specifies if this specimen is the product of a field visit of this project',
  `data_public_access` tinyint(1) DEFAULT NULL COMMENT 'Boolean field (1= yes, 0=no). This field will be used to specify whether the record can be available or not to general public. This will be filled according to data-donor agreement.',
  `filename` text COMMENT 'Dataset file (Excel, CSV, etc.)',
  `username` varchar(15) DEFAULT NULL,
  `source` text,
  `provider_institute_id` text COMMENT 'ID of institute that provided the record / Use valid herbarium and genebanks standard codes',
  `provider_name` text COMMENT 'Name of institute that provided the record / Use valid herbarium and genebanks standard codes',
  `institute_id` text COMMENT 'ID of institute where specimen seen',
  `institute_name` text,
  `is_expert` tinyint(1) DEFAULT NULL COMMENT '1 if the record comes from an expert',
  `collection` text COMMENT 'Name of the collection to which this specimen belongs',
  `source_url` text COMMENT 'Source URL (without format) if coming from internet',
  `botrecat` text COMMENT 'Sight record or vouchered record. e.g. Voucher',
  `availability` tinyint(1) DEFAULT NULL COMMENT 'Availability of germplasm (is the accession truly available to the public?)',
  `image` text COMMENT 'Absolute path where the picture is stored',
  `final_image` varchar(255) DEFAULT NULL COMMENT 'Relative Path of the picture. e.g. IMG6578.JPG',
  `unique_number` varchar(100) DEFAULT NULL COMMENT 'Code given by the institution to each specimen/accession stored. e.g. DC34566',
  `barcode` text COMMENT 'Barcode of the specimen or sample. e.g. A0928873874',
  `vno_1` text COMMENT 'Any other identifier in the specimens. e.g. 9876',
  `vno_2` text COMMENT 'Any other secondary identifier in the specimens',
  `voucher_id` text COMMENT 'ID of the voucher specimens',
  `x1_family` text COMMENT 'The family name appropriate to the Genus name field, entered in full with capitalisation of the first letter only. If the family is unknown leave blank.',
  `x1_genus` text COMMENT 'Generic name should be entered in full with the first letter capitalised.',
  `x1_sp1` text COMMENT 'The species epithet of the plant must be entered in full, all lowercase, no embedded spaces. It may contain one or two hyphens.',
  `x1_author1` text COMMENT 'Use standard author names as given in IPNI',
  `x1_rank1` text COMMENT 'Enter the rank of the second specific epithet if there is one',
  `x1_sp2` text COMMENT 'If the plant represents a new species which has not been formally described, then sp. nov., sp. A, sp. 1 (or other acceptable codes) should be entered, if possible followed by a unique identifier, such as the collector''s name and number or the locality. ',
  `x1_author2` text COMMENT 'Use standard author names as given in IPNI',
  `x1_rank2` text COMMENT 'ter the rank of the second specific epithet if there is one.',
  `x1_sp3` text COMMENT 'As for second species epithet (see column T',
  `x1_author3` text COMMENT 'Use standard author names as given in IPNI',
  `x1_detby` text COMMENT 'Name of determinator (name of a person). This field is used to store the name of the botanist who last named the specimen. The format is "surname, initials". Use a ; to separate two names',
  `x1_detdate` date DEFAULT NULL,
  `x1_detdd` text COMMENT 'Day of determination',
  `x1_detmm` text COMMENT 'Month of determination',
  `x1_detyy` text COMMENT 'Year of determination',
  `x1_detstat` text COMMENT 'Determination source: determinator or folder',
  `x2_family` text,
  `x2_genus` text,
  `x2_sp1` text,
  `x2_author1` text,
  `x2_rank1` text,
  `x2_sp2` text,
  `x2_author2` text,
  `x2_rank2` text,
  `x2_sp3` text,
  `x2_author3` text,
  `x2_detby` text,
  `x2_detdate` date DEFAULT NULL,
  `x2_detdd` text,
  `x2_detmm` text,
  `x2_detyy` text,
  `x2_detstat` text,
  `x3_family` text,
  `x3_genus` text,
  `x3_sp1` text,
  `x3_author1` text,
  `x3_rank1` text,
  `x3_sp2` text,
  `x3_author2` text,
  `x3_rank2` text,
  `x3_sp3` text,
  `x3_author3` text,
  `x3_detby` text,
  `x3_detdate` date DEFAULT NULL,
  `x3_detdd` text,
  `x3_detmm` text,
  `x3_detyy` text,
  `x3_detstat` text,
  `is_hybrid` tinyint(1) DEFAULT NULL COMMENT '1 if record is a hybrid species (accepted name or not). 0 otherwise',
  `hybrid_memo` text COMMENT 'the name of the parents within the hybrid',
  `tnrs_overall_score` float DEFAULT NULL,
  `tnrs_source` text,
  `tnrs_x1_family` text,
  `tnrs_final_taxon` text,
  `tnrs_author1` text,
  `taxon_final` text,
  `f_x1_genus` text,
  `f_x1_sp1` text,
  `f_x1_rank1` text,
  `f_x1_sp2` text,
  `f_x1_rank2` text,
  `f_x1_sp3` text,
  `annotated_specimen` tinyint(1) DEFAULT NULL,
  `type` tinyint(1) DEFAULT NULL COMMENT 'If this is a Type or not (Is a type = Y, isn''t a type = N)',
  `type_memo` text COMMENT 'Type info is different from determinator',
  `collector` text,
  `addcoll` text,
  `collnumber` text,
  `prefix` text,
  `number` text,
  `suffix` text,
  `colldate` date DEFAULT NULL,
  `colldd` text,
  `collmm` text,
  `collyy` text,
  `final_country` text,
  `country` text,
  `iso` text,
  `final_iso2` varchar(2) DEFAULT NULL COMMENT 'This is the ISO of the country that is linked to Countries table',
  `adm1` text COMMENT 'Name of the State/Province where specimen was collected',
  `adm2` text COMMENT 'Name of the County/District/Municipality where specimen was collected',
  `adm3` text COMMENT 'Further administrative level details (level 3)',
  `adm4` text COMMENT 'Further administrative level details (level 4)',
  `local_area` text COMMENT 'Recognised areas smaller than County/District (i.e. National park, Forest Reserves, River deltas)',
  `locality` text COMMENT 'Full locality description',
  `coord` text COMMENT 'Any provided coordinates (any system)',
  `lat_deg` float DEFAULT NULL COMMENT 'Latitude (degrees)',
  `lat_min` float DEFAULT NULL COMMENT 'Latitude (minutes)',
  `lat_sec` float DEFAULT NULL COMMENT 'Latitude (seconds)',
  `ns` varchar(50) DEFAULT NULL,
  `final_ns` enum('N','S') DEFAULT NULL COMMENT 'North or South (N or S)',
  `latitude` float DEFAULT NULL COMMENT 'Latitude in decimal degrees',
  `long_deg` float DEFAULT NULL COMMENT 'Longitude (degrees)',
  `long_min` float DEFAULT NULL COMMENT 'Longitude (minutes)',
  `long_sec` float DEFAULT NULL COMMENT 'Longitude (seconds)',
  `ew` varchar(50) DEFAULT NULL,
  `final_ew` enum('E','W') DEFAULT NULL COMMENT 'East or West (E or W)',
  `longitude` float DEFAULT NULL COMMENT 'Longitude in decimal degrees',
  `llorig` text COMMENT 'Latitude/Longitude original source',
  `lldatum` text COMMENT 'Latitude/Longitude datum (i.e. WGS84)',
  `georef_history_id` int(11) DEFAULT NULL,
  `latitude_georef` float DEFAULT NULL,
  `longitude_georef` float DEFAULT NULL,
  `distance_georef` float DEFAULT NULL,
  `georef_flag` tinyint(1) DEFAULT '0',
  `alt` text,
  `final_alt` double DEFAULT NULL,
  `alt_max` varchar(20) DEFAULT NULL COMMENT 'Maximum altitude (if a range is specified, then the MIN should be in the "alt" field)',
  `final_alt_max` double DEFAULT NULL,
  `cult_stat` text,
  `final_cult_stat` enum('weedy','cultivated','wild') DEFAULT NULL COMMENT 'Weedy, cultivated, wild',
  `origin_stat` text,
  `final_origin_stat` enum('non_native','native','introduced','naturalised') DEFAULT NULL,
  `soil` text COMMENT 'Description of soil conditions at site if available',
  `slope` text COMMENT 'Slope of site if available',
  `aspect` text COMMENT 'Aspect of site if available',
  `habitat_txt` text COMMENT 'Description of habitat',
  `plant_desc` text COMMENT 'Free text description of the plant, including info as: Life Form; Size; Leaves; Stems; Flowers; Fruits; Bark; other unique characters',
  `frequency` text COMMENT 'How abundant is the specimen at the collection site?',
  `fl_code` tinyint(1) DEFAULT NULL COMMENT 'Flowering information',
  `fr_code` tinyint(1) DEFAULT NULL COMMENT 'Fruiting information',
  `inflo_graminea` tinyint(1) DEFAULT NULL COMMENT 'Phenological information (only for Graminae /Poacaea)',
  `vernacular` text COMMENT 'Vernacular (common) name',
  `language` text COMMENT 'Language or tribal name of common name',
  `uses` text COMMENT 'Uses as recorded on label',
  `dups` text COMMENT 'Any other known herbarium codes ',
  `notes` text COMMENT 'Any additional info on the label',
  `comments` text COMMENT 'Use in case you need to register any issue in the digitization of the specimen',
  `timestamp` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `coord_check_old` tinyint(1) DEFAULT NULL,
  `citation` text,
  `final_lat` float DEFAULT NULL,
  `final_lon` float DEFAULT NULL,
  `coord_source` text,
  `taxstand_family` text,
  `taxstand_genus` text,
  `taxstand_sp1` text,
  `taxstand_sp2` text,
  `taxstand_author1` text,
  `taxstand_final_taxon` text,
  `temp_id` int(11) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `image` (`final_image`),
  KEY `taxon_id` (`taxon_id`),
  KEY `index_old_id` (`old_id`),
  FULLTEXT KEY `filename` (`filename`)
) ENGINE=MyISAM AUTO_INCREMENT=593 DEFAULT CHARSET=utf8;
